mongo:
  container_name: mongo
  image: mongo:latest
  restart: always
  ports:
    - 27017
  volumes:
    - ./mongo/data:/data/db
    - ./mongo/mongod.conf:/etc/mongod.conf
  command: --config /etc/mongod.conf
