named:
  container_name: named
  image: xiewulong/bind:latest
  restart: unless-stopped
  ports:
    - 53/tcp
    - 53/udp
  # command: -c /etc/bind/named.conf -u named
  volumes:
    - ./named/named.conf:/etc/bind/named.conf
    - ./named/data:/var/bind
